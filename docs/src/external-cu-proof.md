# Deprecated CU protocol page

See [Externally driven CU sessions](./external-cu-session.md). `cu proof` and
`external_cu_proof` remain deprecated aliases through the same authorization and
executor; legacy receipt vocabulary and digest domains remain stable.
