# Receiver observation study (2026-09-24)

Fixed-count production HTTP receiver inspections, with every refusal and client latency retained. No keyboard dispatch or automatic action retry. Explicit disposable-monitor/browser/setup-click opt-ins only. Separate hermetic tests characterize unchanged human-focus guards before, during and after retained-receiver operations; they do not claim native concurrent-user isolation.
