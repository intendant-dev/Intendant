# One-use retained-receiver ArrowLeft

Follow-up to merged #948. Add fixed unmodified ArrowLeft alongside ArrowRight, using one direction-bound pending preparation and the same receiver, authority, source-retention and cancellation engine. No general keys, text, shortcuts, activation, implicit click, retries or live-daemon deployment. Native acceptance and validation are pending.
