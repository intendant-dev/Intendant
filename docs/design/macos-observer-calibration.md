# Actual observer calibration (2026-09-26)

Calibrate the existing browser-supervisor executable with two event-counter tables, raw bounded samples and observation-access checks. No input, browser, display creation, event tap or permission prompts. Equal counts are not evidence of user inactivity. User-confirmed typing and live input acceptance remain distinct.
