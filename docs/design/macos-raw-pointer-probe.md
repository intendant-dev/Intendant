# Raw pointer probe and concurrent-owner evidence

Next slices after #939: build and inspect a window-addressed raw click without posting by default; separate desktop observations from attributed fixture input. The owner is actively using this Mac. This continuation must not inject global input, change focus, create virtual monitors, or inspect user content.

Raw native delivery is experimental, requires a separate explicit opt-in, targets only the disposable receiver owned by the probe, and must never be exposed as an ordinary daemon capability before independent application acceptance. No human-motion observation alone proves agent interference; unavailable action-level focus still fails closed.
